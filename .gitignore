# Graphify knowledge graph — local only
graphify-out/

# Uploaded broker statements (tradebooks/ledgers) — financial PII, never commit
mis-portal/data/

# Canonical uploads tree (property deeds/plans, art images, bank statements) —
# family financial documents & PII live on disk with paths in the DB. NEVER commit.
uploads/
mis-portal/bank_statements/

# Python
mis-portal/venv/
mis-portal/__pycache__/
mis-portal/*.pyc
mis-portal/.env

# Gmail OAuth — never commit
mis-portal/workers/gmail_credentials.json
mis-portal/workers/gmail_token_central.json

# Broker access tokens — never commit
mis-portal/equity/equity_tokens.json

# Node / Next.js
iws-portal-frontend/node_modules/
iws-portal-frontend/.next/
.next/
iws-portal-frontend/.env.local

# Repo-root npm install (anime.js, for the landing page). Separate from the portal
# build — iws-portal-frontend has its own package.json and lockfile. Leading slash
# so these match ONLY at the root and never shadow a real app's manifest.
/node_modules/
/package.json
/package-lock.json

# Nginx default page
html/
.agents/.claude/
.impeccable/
skills-lock.json
__pycache__/
*.pyc
mis-portal/workers/misc.txt
iws-portal-frontend/AGENTS.md
iws-portal-frontend/CLAUDE.md
.agents/
.claude/
skills-lock.json
.impeccable/
*.xlsx
mis-portal/.cams_browser_profile/
logs/
mis-portal/logs/
mis-portal/reports/
iws-portal-frontend/docs/work-log-*.md
CLAUDE.md
PRODUCT.md
MIS-REPORT.xlsx
gmail_token_central.json.bak-*
*.png
mis-portal/workers/manual_cas_retrigger.py

# Nuvama PMS worker — local runtime artifacts. NEVER commit: the browser
# profile and saved sessions hold live auth cookies (secrets), and downloads
# hold client financial statements.
mis-portal/.nuvama_browser_profile/
mis-portal/.nuvama_sessions/
mis-portal/.pms_downloads/

# Vested / DBS portal scrapers — same rule: browser profiles + saved sessions
# hold live auth cookies, downloads hold client statements, and the feed cache
# holds scraped client holdings/cash. All are local runtime artifacts.
mis-portal/.vested_browser_profile/
mis-portal/.vested_sessions/
mis-portal/.vested_downloads/
mis-portal/.dbs_browser_profile/
mis-portal/.dbs_sessions/
mis-portal/.dbs_downloads/
mis-portal/equity/feeds/

# IBKR Flex statement disk cache — holds account positions + cash; local fallback only
mis-portal/.ibkr_statements/
# IBKR SFTP delivery drop — statements IBKR leaves on their server and we collect.
# Real account positions/cash/trades; this repo is PUBLIC, so never track these.
mis-portal/.ibkr_sftp/
# Drafted provisioning email to IBKR — contains real account numbers. Public repo.
mis-portal/deploy/ibkr/email-to-ibkr.txt

# ICICI Prudential PMS scraper — browser profile holds live auth cookies (secrets);
# local runtime artifact, NEVER commit.
mis-portal/.icici_pms_browser_profile/

motion-audits/
mis-portal/.isin_symbol_map.json
ICICI-PRUDENTIAL-IMAGES
AFTERNRITRADEBOOKS
# Share India uTrade scraper byproducts — session cookies & downloaded reports, never commit
mis-portal/.shareindia_browser_profile/
mis-portal/.shareindia_downloads/

# Local scratch
iws-portal-frontend/tmp/

# Tradebooks and Ledgers
TRADEBOOKS&LEDGERS
PLStatements
# User roster for workers/seed_users.py — real names/emails, never commit
mis-portal/seed_users.json

# .env backups — same secrets as .env (JWT_SECRET, DB/Redis passwords, API keys).
# `mis-portal/.env` was already ignored but its dated backups were not.
mis-portal/.env.bak*
mis-portal/.env.*.bak

# Investor statement drops (NSDL/CAMS CAS, broker tradebooks). Contain PAN, address,
# folio numbers and full holdings — this repo is public. Keep them outside the tree,
# or in a folder matching one of these patterns.
HDR-as-of-31st-July/
*-CAS_*.PDF
*-CAS_*.pdf
NSDLe-CAS*
