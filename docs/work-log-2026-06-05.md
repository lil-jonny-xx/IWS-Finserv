# Worklog — 2026-06-05

## Asset Class Reclassification

### ICICI Prudential Equity Savings Fund
- SEBI category is **Hybrid — Equity Savings**, not Equity
- CAMS CAS reports it as scheme type `EQUITY` causing wrong classification
- Changed `security_type`: `MF_EQUITY` → `MF_HYBRID`
- Changed `asset_class`: `EQUITY` → `HYBRID`
- Created `security_type_override` table in DB to pin per-ISIN classifications
- Updated `cas_parser_worker.upsert_security()` to check override table before writing — prevents CAS automation from reverting the fix on every run

### New Asset Class Buckets: HYBRID and ARBITRAGE
- Added `--chart-hybrid` (purple) and `--chart-arbitrage` (amber) CSS variables in `globals.css` (light + dark)
- Added both to `CLASS_COLORS` and `CLASS_LABELS` in `dashboard/page.tsx` — appear as separate slices in the overview donut chart
- Added both to `ASSET_CLASS_ORDER` and `ASSET_CLASS_LABELS` in `MFTable.tsx` — appear as separate sections in the MF holdings table
- Updated `cas_parser_worker.map_type()`:
  - `HYBRID` scheme type → `("MF_HYBRID", "HYBRID")` (was `EQUITY`)
  - `ARBITRAGE` scheme type → `("MF_ARBITRAGE", "ARBITRAGE")` (was `FIXED_INCOME`)

---

## CAS Automation Audit & Hardening

### Pre-run deep scan (for tonight's 11 PM IST run)
Checked: file permissions, env vars, DB tables, worker imports, Playwright, Gmail token, PID lock, cron entries, log paths, screenshot dir.

**Findings fixed:**
- Browser profile `.cams_browser_profile`: `775` → `700`
- AMFI NAV worker cron: `/var/www/mis-portal/venv/bin/python` → `/var/www/.venv/bin/python`
- FX Rates worker cron: same old-venv fix
- All 8 cron entries now consistently use `/var/www/.venv`

**All clear:**
- All 6 entities with PAN + email in DB
- All DB tables present with data (holding: 17 rows, mf_transaction: 678)
- Gmail token has refresh_token — auto-renews
- No stale PID lock
- `HOME=/tmp` set in CAS cron
- `security_type_override` cursor uses connection-level `RealDictCursor` — dict access works

---

## .env Cleanup

- Removed all `ENTITY_*_PAN` and `ENTITY_*_EMAIL` vars (6 entities × 2 = 12 vars removed)
- Both fields already read from DB (`entity` + `pan_group` tables) — no code referenced these env vars
- Added explanatory comment in their place

---

## Dead Code Removal (`main.py`)

Removed 8 legacy `/api/*` duplicate endpoints (no `/v1/`) — ~450 lines:
- `GET  /api/health`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET  /api/me`
- `GET  /api/entities`
- `GET  /api/holdings`
- `GET  /api/equity/holdings`
- `GET  /api/equity/summary`

Also removed dead `POST /api/v1/auth/refresh` endpoint (never called by frontend).

Note: `write_audit_log()` was flagged by static scan but IS called (login + logout) — kept.

---

## Equity Page Fixes

### "Mkt Value" → "Cur Value" label
- Renamed in summary strip, column header, and sector group subtotals in `EquityTable.tsx`
- Makes it clear this is the current value of the investment, not a historical market snapshot

### Exposure % — entity-wide calculation
- **Bug**: `equity_sync_worker.py` calculated `total_value` as sum of the current broker's holdings only
  - e.g. DHR has Zerodha + Angel One; Zerodha holdings showed exp% vs Zerodha-only total
- **Fix**: Added post-sync recalculation in `main()` after all brokers finish, grouping by `entity_id` only — same SQL already used in `equity_price_worker.update_exposure_pct()`
- Applied fix to current DB values immediately (52 rows updated)

---

## Git

- Removed `.next/trace` and `.next/trace-build` accidentally committed (build artefacts)
- Added `.next/` to root `/var/www/.gitignore` (was only in `iws-portal-frontend/.gitignore`)

---

## Commits

| Message |
|---------|
| `feat: add HYBRID and ARBITRAGE asset class buckets across the system` |
| `feat: CAS hardening, XIRR, txn filter, asset class routing` |
| `chore: remove .next build artefacts, add .next/ to root .gitignore` |
| `fix: browser profile chmod 700, cron venv paths to /var/www/.venv` |
| `refactor: remove legacy /api/* duplicate endpoints and dead /api/v1/auth/refresh` |
| `fix: equity exp% vs entity portfolio, cur value label, remove legacy API endpoints` |
