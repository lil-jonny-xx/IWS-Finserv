# Worklog — 2026-06-24

## Tradebook / ledger import + transaction-derived metrics + live txn sync

Backfill broker tradebooks/ledgers and keep them current from the broker APIs, so cost-basis, XIRR/CAGR/YTD and portfolio money-weighted return compute daily.

- **`import_tradebooks_multi.py`** — layout-aware tradebook importer (Zerodha/Angel/Dhan/Vested) → `stock_transaction`, deduped on `source_ref`.
- **`symbol_bridge.py`** — resolve Angel/Dhan company names → ISIN via holdings reconstruction.
- **`equity_txn_metrics_worker.py`** — FIFO lot reconstruction → xirr/cagr/ytd backfill into `equity_holding` (+ Vested `foreign_equity_holding`, measured from purchase date).
- **`import_ledgers.py` + `db_migrate_ledgers.py`** — cash ledgers → `cash_ledger` (daily balances) and `external_cashflow` (deposits/withdrawals/dividends).
- **`portfolio_returns_worker.py`** — per-entity money-weighted portfolio XIRR with a coverage flag.
- **`broker_txn_sync_worker.py`** — daily live trade sync (Zerodha/Angel fills, Dhan history) + Dhan ledger; adds `fetch_trades()`/`fetch_ledger()` to the broker modules.
- Gitignore `mis-portal/data/` (uploaded broker statements = PII).

## Commits

| Message |
|---------|
| `feat(equity): tradebook/ledger import + transaction-derived metrics + live broker txn sync` |
