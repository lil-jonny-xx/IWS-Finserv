# Python dependencies for the MIS portal backend.
# Install into the deployment venv:  /var/www/.venv/bin/pip install -r requirements.txt
#
# Note: the API/worker runtime depends on more than this (fastapi, uvicorn, psycopg2,
# redis, slowapi, pyjwt, bcrypt, python-dotenv, openpyxl, ...). This file pins the
# packages the Jarvis assistant adds plus the numeric stack it relies on.
anthropic>=0.109  # engine.py uses web_search_20260209 / code_execution_20260120 / output_config / adaptive thinking — older SDKs lack these
numpy>=1.26
pandas>=2.0
python-multipart>=0.0.20  # FastAPI file uploads (bank-statement upload endpoint)
yfinance>=1.4
